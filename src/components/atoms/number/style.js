import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 0.5,
    justifyContent: 'center',
    alignItems: 'center',
  },
  number : {
    fontSize: 24,
    color: '#52528C',
    marginVertical: 10,
    fontWeight: 'bold',
  }
  });
